window.addEventListener('load', () => {
    const image = document.getElementById('fade-slide-image');
    image.style.opacity = 1; /* Trigger the fade-in */
    image.style.transform = 'translateY(0)'; /* Slide up to its final position */
});
